# Инструкция для Агента: Создание лога действий (tasks/T-XXXX.md)

**Твоя роль:** Ты формируешь технический лог действий для обеспечения наблюдаемости (Observability).

## 🛑 Базовые правила (DoD-Driven Flow)
1. **Лог действий:** Создавай файл `tasks/T-XXXX.md` (где XXXX — номер Issue) в новой ветке, когда переводишь задачу в `status:in-progress`.
2. **Один PR:** Код, тесты, изменения в `spec.md` и сам файл `tasks/T-XXXX.md` отправляются в одном Pull Request.
3. **Краткость:** Лог должен содержать 3–7 шагов, которые ты выполнил или планируешь выполнить.

## 📝 Шаблон файла T-XXXX.md
Строго используй эту структуру:

```markdown
# T-XXXX: [Название задачи]

**Issue:** #[Номер]
**Spec:** [R-XXX | New requirement | n/a]

> **Когда использовать `n/a`?**
> Для задач, которые **не изменяют поведение системы** и не затрагивают `spec.md`.
> Это типично для `chore`/инфраструктуры/техдолга, рефакторинга, написания тестов,
> обновления CI, документации и т.п. В таких случаях в поле `Why` следует раскрыть
> мотивацию.

## Why
* [1-2 предложения: зачем это нужно бизнесу или системе?]

## Goal
* [Что конкретно изменится после выполнения?]

## Plan
* [ ] Шаг 1: [Например, добавить R-XXX в spec.md]
* [ ] Шаг 2: [Написать падающий тест для R-XXX]
* [ ] Шаг 3: [Реализовать логику в src/...]
* [ ] Шаг 4: [Проверить прохождение тестов]

## Acceptance
* [ ] [Критерий 1: проверяемый результат]
* [ ] [Критерий 2: успешный тест]
```

## 🔄 Сценарии работы
* **Старт задачи:** Создай ветку -> Напиши лог в `T-XXXX.md` -> Напиши код/тесты -> Открой финальный PR.
* **Test-First:** Всегда включай шаг написания/обновления тестов *до* шага реализации логики.
* **Изменение требований в процессе:** Если во время написания кода (статус `in-progress`) ты понимаешь, что нужно сменить подход — обнови `T-XXXX.md` в том же PR, чтобы команда видела актуальный ход мыслей.

## 🧹 Особые случаи: chore / техдолг / инфраструктура
Когда задача не меняет пользовательского поведения и не затрагивает `spec.md`, процесс:

1. **Issue и заголовок.** Используй префикс `chore(area): …` (пример: `chore(db): migrate to connection pooling`).
2. **PR.** Создаёшь `tasks/T-XXXX.md`, описываешь саму работу и, важно, как будешь доказывать, что поведение **не изменилось** (через прогон существующих тестов).
3. **Тело PR.**
   * `Fixes: #123` (номер issue)
   * `Spec: n/a` (или `Internal change`)
   * `Tasks Log: tasks/T-XXXX.md`
   * `Why:` 2–5 строк технической мотивации; для chore‑задач это ключевой текст.