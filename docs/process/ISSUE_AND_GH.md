# Как оформлять issue и работать с ними через GitHub CLI (gh)

## Зачем это нужно ✅
Хорошо оформленные issue ускоряют обсуждение и внедрение фич/фиксов, а `gh` позволяет управлять ими быстро из терминала — удобно для локальной разработки и CI.

---

## 1) Структура хорошего issue ✍️
- **Заголовок**: коротко и в формате `type(scope): short description` (например: `feat(integration): Zima UDP NMEA telemetry`).
- **Описание**: что, зачем, ожидаемое поведение.
- **Контекст/спецификация**: ссылки на спецификации/доки (например: `docs/features/zima-integration.md`).
- **Требования**: список конкретных пунктов (checklist) — тесты, документация, интерфейсы.
- **Критерии приёмки**: что должно быть выполнено для закрытия issue.
- **Метки**: выберите подходящие (bug, enhancement, test, help wanted и т.п.).
- **Ассайны/приоритет**: кто ответственный, сроки/приоритет (P1/P2).

> 💡 Совет: добавляйте пример команды/данных и минимальный repro, если это баг.

---

## 2) Быстрое создание issue локально (через `gh`) 🧾
Создать issue с заголовком и текстом из файла:

```bash
# из корня репозитория
gh issue create --title "feat(example): short title" --body-file path/to/body.md --label "enhancement"
```

Пример: использовать подготовленный файл в `docs/features`:

```bash
gh issue create --title "feat(integration): Zima UDP NMEA GGA telemetry service" \
  --body-file docs/features/zima-integration.md --label "enhancement"
```

---

## 3) Управление issue через `gh` 🔧
- Редактировать тело/заголовок:

```bash
gh issue edit <NUMBER> --title "new title" --body-file new_body.md
```

- Добавить/удалить метку:

```bash
gh issue edit <NUMBER> --add-label "test" --remove-label "help wanted"
```

- Назначить исполнителя:

```bash
gh issue edit <NUMBER> --add-assignee username
```

- Закрыть/переоткрыть:

```bash
gh issue close <NUMBER>
gh issue reopen <NUMBER>
```

- Добавить комментарий:

```bash
gh issue comment <NUMBER> --body "Текст комментария"
```

- Просмотр issue в терминале:

```bash
gh issue view <NUMBER> --web   # открыть в браузере
gh issue view <NUMBER>         # показать в терминале
gh issue list --label "enhancement" --state open
```

---

## 4) Как связать issue с PR и коммитами ⛓️
- Создайте ветку по issue: `git checkout -b feat/zima-123` (номер/ключ в названии полезен).
- В сообщении коммита можно ссылаться `Refs #4`, `Closes #4` — `Closes` автоматически закроет issue при мерже.

Пример:

```bash
git checkout -b feat/zima-integration
# сделать изменения
git commit -m "feat(zima): add zima service (Closes #4)"
git push -u origin feat/zima-integration
gh pr create --title "feat(zima): ..." --body "Implements X — closes #4" --base main
```

---

## 5) Шаблоны issue (Issue templates) 📋
- Помогают стандартизировать ввод. Размещайте шаблоны в `.github/ISSUE_TEMPLATE/` с фронтматтером YAML, например:

```yaml
---
name: "Feature request"
about: "Request a new feature"
labels: [enhancement]
---

**Описание**

Что вы предлагаете?
```

---

## 6) Рекомендации по рабочему процессу 🛠️
- Привязывайте небольшие, одноцелевые issue — проще планировать и ревьюить PR. ✅
- Используйте чеклисты в теле issue для трекинга подзадач.
- Помечайте, какие документы/контракты нужно обновить (например `src/platform/contracts.ts` при добавлении настроек).
- Для инфраструктурных задач (скрипты/CI/simulators) — создавайте отдельные issue и связывайте с основной задачей.

---

## 7) Правила наименования веток 🌿
- Формат: `<type>/<short-description>-<issue-number>` или `<type>/<scope>/<short-description>-<issue-number>`.
- `type` — одно из: `feat`, `fix`, `chore`, `docs`, `test`, `refactor`, `hotfix`.
- Примеры:
  - `feat/zima-4`
  - `fix/map/marker-null-12`
  - `chore/docs/issue-templates-7`
- Используйте короткие и понятные описания, дефисы `-` для разделения слов, не используйте пробелы и большие буквы.

---

## 8) Формат сообщений коммитов (Conventional Commits) ✍️
- Формат: `<type>(<scope>): <short summary>`
  - `type` — `feat`, `fix`, `chore`, `docs`, `style`, `refactor`, `perf`, `test`, `ci`.
  - `scope` — опционально, например `platform`, `zima`, `ui`.
- Примеры:
  - `feat(zima): add UDP listener and GGA parser (Closes #4)`
  - `fix(map): clamp latitude bounds`
- Тело сообщения (по необходимости): опишите мотивацию, изменение API, migration notes.
- Footer: используйте `Closes #<issue>` или `Refs #<issue>` для автоматического закрытия issue при мерже.

---

## 9) Правила оформления PRs ✅
- Title: в стиле коммитов: `feat(zima): implement service`.
- Body: краткое описание, что сделано, почему, какие файлы/контракты обновлены; включите чеклист (tests, docs, changelog).
- Обязательно укажите в тексте PR `Closes #<issue-number>` чтобы при мерже issue автоматически закрывался.
- Привязывайте ревьюеров и назначайте assignees при необходимости.
- При PR, влияющем на публичные API/контракты, добавьте заметку в CHANGELOG или docs.

---

## 10) Частые команды (шпаргалка) 📌
- List issues: `gh issue list --state open`
- Create: `gh issue create --title "..." --body-file file.md`
- Edit: `gh issue edit 4 --add-label test`
- Comment: `gh issue comment 4 --body "..."`
- Close: `gh issue close 4`
- Open in browser: `gh issue view 4 --web`
- Create PR: `gh pr create --fill --base main` (если коммит содержит `Closes #<issue>`, `--fill` подхватит описание)

---

Если нужно, могу добавить шаблон PR/commit-msg hook или пример `pull_request_template.md`. Хотите, чтобы я добавил это в репозиторий сейчас?