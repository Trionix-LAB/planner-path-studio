# T-115: FlyTo zoom to 50m grid step

**Issue:** local-fix (no GitHub issue)
**Spec:** updated first (R-002 addition)

## Goal
- Ensure every `flyTo/Переместиться к` action sets zoom to a level corresponding to `50 м` grid step.
- Scope: agent, base station, map objects, raster overlays, vector overlays.

## Plan
- [x] Update `spec/spec.md` with a single zoom contract for all flyTo actions.
- [x] Add shared flyTo zoom utility based on `50 м` target grid step.
- [x] Apply utility in map workspace flyTo handlers (agent/base/raster/vector).
- [x] Apply utility in map object center request handling.
- [x] Add unit tests for flyTo zoom utility.
- [x] Run tests.

## Validation
- [x] `npx vitest run src/test/flyToZoom.test.ts`
- [x] `npm run test`
- [x] `npm run lint` (no errors, 2 existing warnings in `MapWorkspace.tsx`)
