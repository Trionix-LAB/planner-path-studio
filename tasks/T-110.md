# T-110: Инструмент «Измерить» — измерение расстояния между двумя точками

**Issue:** #110
**Branch:** `feat/map-110-measure-tool`
**Spec:** R-064 (новое требование)

---

## Why

Пользователям необходимо измерять расстояния на карте прямо в приложении, без внешних инструментов. Измерение должно быть полноценным объектом миссии: сохраняться, редактироваться (название, цвет, описание) и удаляться.

## Goal

Добавить кнопку «Измерить» в блок «Инструменты» TopBar. При активации: первый клик фиксирует начальную точку, второй — конечную; на карте отрисовывается пунктирная линия с подписью расстояния в метрах. Объект «Измерение» появляется в списке объектов и редактируется в правой панели свойств. ПКМ по кнопке «Измерить» — диалог «Удалить все измерения?».

---

## Архитектурный контекст

> Читай внимательно — здесь всё, что нужно для реализации без лишних исследований.

### Ключевые файлы

| Файл | Роль |
|---|---|
| `src/features/map/model/types.ts` | `Tool` и `MapObjectType` union — сюда добавить `"measure"` |
| `src/components/map/TopToolbar.tsx:154–159` | Массив `tools[]` — добавить кнопку «Измерить» |
| `src/pages/MapWorkspace.tsx:685` | `useState<Tool>('select')` — активный инструмент |
| `src/components/map/MapCanvas.tsx:961–983` | `handleMapClick` — switch по активному инструменту |
| `src/components/map/MapCanvas.tsx:1026–1048` | `useEffect` при смене инструмента — очищать `measurePoints` |
| `src/components/map/MapCanvas.tsx:1068–1079` | Esc — добавить очистку `measurePoints` |
| `src/components/map/MapCanvas.tsx:602–623` | `segmentLengthIcon` — переиспользовать для подписи расстояния |
| `src/components/map/MapCanvas.tsx:1184` | `haversineDistanceMeters` — переиспользовать для вычисления |
| `src/components/map/MapObjectProperties.tsx` | Панель свойств — добавить ветку для `type === 'measure'` |
| `src/components/map/LeftPanel.tsx:748–808` | Список объектов — `measure` объекты появятся автоматически |
| `src/components/map/MapContextMenu.tsx` | Переиспользовать для контекстного меню TopBar-кнопки |

### Архитектурный паттерн

Приложение использует **prop-drilling** (без Zustand/Redux):

```
MapWorkspace (все useState)
  ├── TopToolbar ← activeTool, onToolChange
  ├── LeftPanel  ← objects[], selectedObjectId, onObjectSelect, onObjectDelete
  ├── MapCanvas  ← activeTool, objects[], onObjectCreate, onObjectUpdate, onObjectDelete
  │     ├── MapEvents → handleMapClick → switch(activeTool) → onObjectCreate(...)
  │     └── <Polyline>, <Marker icon={L.divIcon}> для рендера
  └── RightPanel ← selectedObject → MapObjectProperties → onSave(updatedObject)
```

**Цикл создания объекта:**
1. `TopToolbar.onToolChange('measure')` → `MapWorkspace.setActiveTool('measure')`
2. Клик на карте → `MapCanvas.handleMapClick` → при втором клике → `onObjectCreate({ type: 'measure', ... })`
3. `MapWorkspace.handleObjectCreate` → `setObjects([...objects, newObj])` → `setSelectedObjectId(newObj.id)` → `setActiveTool('select')`

### Как работает Leaflet-рендер

- Линии: `<Polyline positions={[latlng1, latlng2]} color={obj.color} />`
- Подписи: `<Marker position={midpoint} icon={segmentLengthIcon('123.45 м')} interactive={false} />`
- `segmentLengthIcon` уже реализован в `MapCanvas.tsx:602–623` — **переиспользуй**.
- Расстояние: функция `haversineDistanceMeters(p1, p2)` уже есть в `MapCanvas.tsx:1184` — **переиспользуй**.

### Правый клик на кнопке TopBar

`TopToolbar.tsx:510–513` — уже есть `onContextMenu` на кнопках инструментов, вызывает `onOpenCoordinateBuilder?.(tool.id)`.
Для «Измерить» нужна **другая** callback-пропса — `onMeasureClearAll?: () => void` — или обобщённый `onToolContextMenu(toolId)`.

Диалог подтверждения: переиспользуй существующий `AlertDialog` из shadcn/ui (уже есть в codebase, используется в других местах).

---

## Implementation Plan

### Шаг 1 — Типы (`src/features/map/model/types.ts`)

- [x] Добавить `"measure"` в `Tool` union.
- [x] Добавить `"measure"` в `MapObjectType` union.
- [x] Добавить ветку геометрии: `{ type: "measure"; points: [GeoPoint, GeoPoint] }` в `MapObjectGeometry`.

### Шаг 2 — Spec и документация

- [x] Добавить **R-064** в `spec/spec.md`:
  ```
  R-064 — Инструмент «Измерить»:
    - MUST: кнопка «Измерить» в блоке «Инструменты» TopBar.
    - MUST: активация → первый клик (начало) → ведение линии до курсора → второй клик (конец).
    - MUST: отрисовка прямой линии с подписью расстояния в метрах (2 знака после запятой).
    - MUST: измерение сохраняется как объект миссии (тип measure) с полями name, note, color, geometry.
    - MUST: свойства объекта (название, описание, цвет) редактируются в правой панели.
    - MUST: ПКМ по кнопке «Измерить» → подтверждение «Удалить все измерения?».
    - MUST: Esc отменяет незавершённое измерение.
    - MUST: линия и подпись корректно масштабируются при зуме/панораме.
  ```
- [x] Добавить «Измерить» в `docs/screens.md` в блок «Инструменты» TopBar и описать UX-поведение.

### Шаг 3 — Кнопка в TopToolbar (`src/components/map/TopToolbar.tsx`)

- [x] Импортировать иконку `Ruler` из `lucide-react`.
- [x] Добавить в массив `tools[]`:
  ```ts
  { id: 'measure' as Tool, icon: Ruler, label: 'Измерить' }
  ```
- [x] Добавить пропс `onMeasureClearAll?: () => void` в интерфейс `TopToolbarProps`.
- [x] На кнопке «Измерить» добавить `onContextMenu` handler:
  - `e.preventDefault()`
  - вызвать `onMeasureClearAll?.()`
  - **не** вызывать `onOpenCoordinateBuilder` (это не про координаты)

### Шаг 4 — Состояние в MapCanvas (`src/components/map/MapCanvas.tsx`)

- [x] Добавить `useState<L.LatLng[]>([])` для `measurePoints` (первый клик — 1 точка, второй — финализация).
- [x] В `handleMapClick` добавить ветку:
  ```ts
  } else if (activeTool === 'measure') {
    if (measurePoints.length === 0) {
      setMeasurePoints([latlng]);
    } else {
      // Два клика — создаём объект
      const p1 = measurePoints[0];
      const p2 = latlng;
      const dist = haversineDistanceMeters(p1, p2);
      onObjectCreate({
        type: 'measure',
        points: [
          { lat: p1.lat, lon: p1.lng },
          { lat: p2.lat, lon: p2.lng },
        ],
        distanceMeters: dist,
      });
      setMeasurePoints([]);
    }
  }
  ```
- [x] В `useEffect` по смене `activeTool` добавить очистку `measurePoints` при уходе с `'measure'`.
- [x] В обработчике `Esc` добавить очистку `measurePoints`.

### Шаг 5 — Рендер измерения на карте (`src/components/map/MapCanvas.tsx`, JSX)

- [x] Рендерить **превью** во время рисования (когда `activeTool === 'measure'` и `measurePoints.length === 1`):
  - `<Polyline positions={[measurePoints[0], cursorPosition]}  color="orange" dashArray="6" />`
  - для `cursorPosition` использовать `useMapEvents({ mousemove })` (аналогично существующему preview).
- [x] Рендерить **финальные объекты** типа `measure` из `objects[]`:
  ```tsx
  {objects
    .filter(o => o.type === 'measure' && o.geometry?.type === 'measure')
    .map(obj => {
      const [p1, p2] = obj.geometry.points;
      const midLat = (p1.lat + p2.lat) / 2;
      const midLon = (p1.lon + p2.lon) / 2;
      const dist = haversineDistanceMeters(
        L.latLng(p1.lat, p1.lon),
        L.latLng(p2.lat, p2.lon)
      );
      return (
        <React.Fragment key={obj.id}>
          <Polyline
            positions={[[p1.lat, p1.lon], [p2.lat, p2.lon]]}
            color={obj.color ?? '#f97316'}
            weight={2}
            eventHandlers={{ click: () => onObjectSelect(obj.id) }}
          />
          <Marker
            position={[midLat, midLon]}
            icon={segmentLengthIcon(`${dist.toFixed(2)} м`)}
            interactive={false}
          />
        </React.Fragment>
      );
    })}
  ```

### Шаг 6 — Создание объекта в MapWorkspace (`src/pages/MapWorkspace.tsx`)

- [x] В `handleObjectCreate` добавить ветку для `type === 'measure'`:
  ```ts
  case 'measure': {
    const count = objects.filter(o => o.type === 'measure').length + 1;
    const newObj: MapObject = {
      id: crypto.randomUUID(),
      type: 'measure',
      name: `Измерение ${count}`,
      visible: true,
      color: '#f97316',
      geometry: {
        type: 'measure',
        points: payload.points,
      },
      note: '',
    };
    setObjects(prev => [...prev, newObj]);
    setSelectedObjectId(newObj.id);
    setActiveTool('select');
    break;
  }
  ```
- [x] Передать `onMeasureClearAll` в `<TopToolbar>`:
  ```ts
  onMeasureClearAll={() => setObjects(prev => prev.filter(o => o.type !== 'measure'))}
  ```
- [x] Но перед удалением показать диалог подтверждения. Добавить `useState<boolean>(false)` для `showClearMeasuresDialog`. При `onMeasureClearAll` → открыть диалог. В диалоге кнопка «Удалить» → `setObjects(...)`.

### Шаг 7 — Диалог подтверждения «Удалить все измерения?»

- [x] В `MapWorkspace.tsx` (или как отдельный inline-компонент) добавить `AlertDialog` из shadcn/ui:
  ```tsx
  <AlertDialog open={showClearMeasuresDialog} onOpenChange={setShowClearMeasuresDialog}>
    <AlertDialogContent>
      <AlertDialogHeader>
        <AlertDialogTitle>Удалить все измерения?</AlertDialogTitle>
        <AlertDialogDescription>
          Все объекты типа «Измерение» будут удалены без возможности восстановления.
        </AlertDialogDescription>
      </AlertDialogHeader>
      <AlertDialogFooter>
        <AlertDialogCancel>Отмена</AlertDialogCancel>
        <AlertDialogAction onClick={handleClearAllMeasures}>Удалить</AlertDialogAction>
      </AlertDialogFooter>
    </AlertDialogContent>
  </AlertDialog>
  ```

### Шаг 8 — Свойства объекта («Измерить» в правой панели)

В `src/components/map/MapObjectProperties.tsx` для `object.type === 'measure'`:

- [x] Отображать поле **Название** (уже общий `Input`, работает для всех типов).
- [x] Отображать поле **Описание** (`Textarea`, аналог маршрута).
- [x] Отображать **цвет линии** (`Input type="color"`).
- [x] Отображать **расстояние** как read-only лейбл:
  ```tsx
  {object.type === 'measure' && object.geometry?.type === 'measure' && (
    <div className="text-sm text-muted-foreground">
      Расстояние: {haversineDistanceMeters(...).toFixed(2)} м
    </div>
  )}
  ```
- [x] Убедиться, что поля редактируются и `onSave` вызывается корректно (уже работает через `handleSave`).

### Шаг 9 — Тесты

- [x] `src/test/measureTool.test.ts` — unit-тест: `haversineDistanceMeters` возвращает корректное расстояние для двух известных точек.
- [x] `src/test/measureTool.test.tsx` (RTL) — тест компонента `TopToolbar`: кнопка «Измерить» рендерится, при клике вызывается `onToolChange('measure')`, при ПКМ — `onMeasureClearAll`.

### Шаг 10 — Проверка

- [x] `npm run typecheck` — 0 ошибок.
- [x] `npm run lint` — 0 ошибок.
- [x] `npm run test` — все тесты зелёные.
- [x] Ручная проверка: активировать «Измерить», кликнуть две точки → линия с подписью; свойства редактируются; Esc отменяет; ПКМ → диалог → удаление.

---

## Acceptance (из Issue #110)

- [x] Кнопка «Измерить» отображается в блоке «Инструменты» TopBar рядом с Маршрут/Зона/Маркер.
- [x] При активации инструмента курсор меняется на крестик.
- [x] Первый клик на карте фиксирует начальную точку; от неё тянется линия-превью до курсора.
- [x] Второй клик фиксирует конечную точку; отрисовывается пунктирная линия с подписью расстояния в метрах (2 знака).
- [x] Объект «Измерение» появляется в списке объектов левой панели.
- [x] В свойствах объекта доступны поля: название, описание, цвет линии.
- [x] ПКМ по кнопке «Измерить» → диалог подтверждения → удаление всех измерений.
- [x] Esc отменяет незавершённое измерение и возвращает в режим Выбор/Редактирование.
- [x] Линия и подпись корректно масштабируются при зуме/панораме.
- [x] Инструмент не ломает существующее поведение других инструментов и селекции.
- [x] `spec/spec.md` обновлён (R-064).
- [x] `docs/screens.md` обновлён.

## Links

PR: (добавить после реализации)
