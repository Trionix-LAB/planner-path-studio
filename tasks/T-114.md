# T-114: Raster render notifications + decode diagnostics + zoom performance

**Issue:** local-fix (no GitHub issue)
**Spec:** updated first (R-058 additions)

## Goal
- Add per-raster render progress notifications.
- Replace generic decode error with categorized user-facing diagnostics.
- Improve zoom responsiveness when large raster overlays are visible.

## Plan
- [x] Update `spec/spec.md` first (R-058 additions).
- [x] Implement per-overlay render start/success notifications.
- [x] Implement categorized raster decode error messages.
- [x] Implement zoom performance mitigation for large rasters.
- [x] Run typecheck/lint/tests/build.

## Validation
- [x] `npm run typecheck`
- [x] `npm run lint`
- [x] `npx vitest run src/test/parseGeoTiff.test.ts src/test/rasterTfwFixtures.test.ts`
- [x] `npm run build`

## Follow-up
- [x] Increased toast stack limit (src/hooks/use-toast.ts) from 1 to 50 so raster notifications are displayed separately per item in batch import.
- [x] On mission open, mark visible persisted raster overlays for per-item render notifications (same flow as post-import).
- [x] Keep the in-progress render toast visible until render completes or fails by using long-lived progress toast handles and explicit dismiss on terminal states.


